[project]
name = "orto"
version = "0.1.0"
description = "Quando devo bagnare l'orto?"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.4",
    "openmeteo-requests>=1.7.5",
    "python-telegram-bot>=22.7",
    "requests-cache>=1.3.1",
    "retry-requests>=2.0.0",
]
